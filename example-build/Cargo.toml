[package]
name = "example-build"
version = "0.0.5"
authors = ["Adam H. Leventhal <ahl@oxidecomputer.com>"]
edition = "2018"

[dependencies]
serde = "1.0"

[build-dependencies]
schemars = "0.8"
serde_json = "1.0"
typify = { path = "../typify" }
