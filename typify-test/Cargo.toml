[package]
name = "typify-test"
version = "0.0.0"
authors = ["Adam H. Leventhal <ahl@oxidecomputer.com>"]
edition = "2018"

[dependencies]
serde = "1.0"

[build-dependencies]
schemars = "0.8.10"
serde = "1.0"
serde_json = "1.0"
typify = { path = "../typify" }
